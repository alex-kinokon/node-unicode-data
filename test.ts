import codepoints from "./output/unicode-16.0.0/General_Category/Uppercase_Letter/code-points.js"

// Get an array of code points in a given Unicode category:
import codePoints from './output/unicode-16.0.0/General_Category/Uppercase_Letter/code-points.js';
// Get an array of symbols (strings) in a given Unicode category:
import symbols from './output/unicode-16.0.0/General_Category/Uppercase_Letter/symbols.js';
// Get a regular expression that matches any symbol in a given Unicode category:
import regex from './output/unicode-16.0.0/General_Category/Uppercase_Letter/regex.js';
// Get the canonical category a given code point belongs to:
// (Note: U+0041 is LATIN CAPITAL LETTER A)
import category from './output/unicode-16.0.0/General_Category' // ).get(0x41;
// Get an array of all code points with a given bidi class:
import on from './output/unicode-16.0.0/Bidi_Class/Other_Neutral/code-points.js';
// Get a map from code points to bidi classes:
import bidiClassMap from './output/unicode-16.0.0/Bidi_Class';
// Get the directionality of a given code point:
import directionality from './output/unicode-16.0.0/Bidi_Class' // ).get(0x41;
import directionality2 from './output/unicode-16.0.0/Sequence_Property/Basic_Emoji/index.js';

// What glyph is the mirror image of `«` (U+00AB)?
import mirrored from './output/unicode-16.0.0/Bidi_Mirroring_Glyph' // ).get(0xAB;

// Get a regular expression that matches all opening brackets:
import openingBrackets from './output/unicode-16.0.0/Bidi_Paired_Bracket_Type/Open/regex.js';

// …you get the idea.

codePoints